spring.application.name=CoWrite
spring.datasource.url=jdbc:mysql://co-write-co-write.a.aivencloud.com:19320/CoWrite
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_6nfJz3xJGFm8f6ePDlS
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
server.port=8888
# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:cert/certificate.p12
# The password used to generate the certificate
server.ssl.key-store-password=ali
# The alias mapped to the certificate
server.ssl.key-alias=certificate
server.ssl.enabled=true
spring.config.import=optional:file:.env[.properties]
JWT_SECRET_KEY=${JWT_SECRET_KEY}
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

